/** @type {import('next').NextConfig} */
const nextConfig = {
  /*   output: 'export',
    images: {
        domains: ['your-image-host.com', 'firebasestorage.googleapis.com'], // Add external image sources if needed
        unoptimized: true, // Disable Next.js default optimization since Firebase does not support it
    }, */
};

export default nextConfig;
